from smbus2 import SMBus as bus

class Antenna:

    def __init__(self,adress,busNum):
        self.id = adress
        self.bus = SMBus(busNum)

    def readHeading():
        heading = bus.read_i2c_block_data(id,1,2)
        return heading
