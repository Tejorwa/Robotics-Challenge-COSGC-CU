from smbus2 import SMBusWrapper, ic_msg

class Antenna:

    def __init__(self,adress):
        self.id = adress

    def readHeading():
        msg = i2c_msg.read(id,2)

        with SMBusWrapper(1) as bus:
            bus.i2c_rdwr(msg)

        a = ord(msg.buf[0]);
        a = a<<8
        b = ord(msg.buf[1]);

        return (a+b)
