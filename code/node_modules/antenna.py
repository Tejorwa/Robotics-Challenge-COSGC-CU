from smbus2 import SMBus as bus

class Antenna:

    id
    bus

    def __init__(self,adress,busNum):
        self.id = adress
        self.bus = SMBus(busNum)

    def readHeading():
        heading = bus.read_byte_data(id,0)
        return heading
